{
  "name": "myip",
  "version": "1.0.0",
  "devDependencies": {
    "wrangler": "^4.115.0",
    "prettier": "3.9.6"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "update": "npm outdated; npm install -g npm-check-updates; ncu; ncu -u; npm install"
  }
}
