{
  "name": "myip",
  "version": "1.0.0",
  "devDependencies": {
    "prettier": "3.7.4",
    "wrangler": "^4.59.2"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "update": "npm outdated; npm install -g npm-check-updates; ncu; ncu -u; npm install"
  }
}
