{
  "private": true,
  "name": "{{ project-name }}",
  "version": "1.0.0",
  "description": "Show myip on Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ authors }}",
  "license": "All Rights Reserved",
  "dependencies": {
    "@cloudflare/wrangler": "^1.19.9"
  }
}
