{
  "name": "myip",
  "version": "1.0.0",
  "devDependencies": {
    "prettier": "3.8.0",
    "wrangler": "^4.58.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "update": "npm outdated; npm install -g npm-check-updates; ncu; ncu -u; npm install"
  }
}
